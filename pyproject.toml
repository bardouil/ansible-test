[project]
name = "ansible-test"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ansible>=10",
    "ansible-lint>=24",
    "molecule>=25",
    "molecule-plugins[docker]>=23",
    "pytest-testinfra>=10",
    "docker>=7",
]
